import { allowlist as n } from "./allowlist.js";
import r from "js-cookie";
/**
 * @preserve
 * Checks whether given cookie name is part of the allowlist, and,
 * if so, returns that cookie (Cookie). If not, returns undefined
 * @param name - The name of the cookie
 */
var t = function (r) {
    return n.find(function (n) {
      return n.name === r;
    });
  },
  o = function (n) {
    return void 0 !== t(n);
  },
  i = function (n) {
    return o(n) ? r.get(n) : void 0;
  },
  e = function () {
    return r.get();
  };
/**
 * @preserve
 * Checks whether given cookie name is part of the allowlist and
 * returns boolean
 * @param name - The name of the cookie
 */
export { i as get, e as getAll, t as getCookieFromAllowlist, o as isCookieInAllowlist };