import { NotificationContext } from '../../contexts/NotificationContext/NotificationContext.js';
import { useContext } from 'react';
const useNotification = () => {
  const context = useContext(NotificationContext);
  if (!context) {
    throw new Error('You need to wrap your app with the NotificationsContainer from @jimdo/notifcations');
  }
  return context;
};
export { useNotification };