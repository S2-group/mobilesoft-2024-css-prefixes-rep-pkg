'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var _tslib = require('../../_virtual/_tslib.js');

/**
 * Returns the pages for advanced pagination.
 *
 * @param {number} dataLength Length of the data we need to show.
 * @param {number} dataPerPage  of data to be shown per page.
 * @param {number} currentPage The active page.
 * @returns {(string | number)[]} Pages to show.
 */
var getPagesToShow = function (dataLength, dataPerPage, currentPage) {
    var ellipsis = '...';
    var pagesToShow = [];
    var pages = Array.from({ length: Math.ceil(dataLength / dataPerPage) }, function (_, i) { return i + 1; });
    if (pages.length <= 5) {
        pagesToShow = pages;
    }
    else if (currentPage < 4) {
        pagesToShow = [1, 2, 3, 4, ellipsis, pages.length];
    }
    else if (currentPage > pages.length - 3) {
        pagesToShow = _tslib.__spreadArray([1, ellipsis], pages.slice(pages.length - 4), true);
    }
    else {
        pagesToShow = _tslib.__spreadArray(_tslib.__spreadArray([
            1,
            ellipsis
        ], pages.slice(currentPage - 2, currentPage + 1), true), [
            ellipsis,
            pages.length,
        ], false);
    }
    return pagesToShow;
};

exports.getPagesToShow = getPagesToShow;
//# sourceMappingURL=advancedPagination.js.map
