"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.handleLocalStorage = exports.getParameterByName = void 0;
var getParameterByName = function (name, queryString) {
    var rename = name.replace(/[[]/, '\\[').replace(/[\]]/, '\\]');
    var regex = new RegExp("[\\?&]".concat(rename, "=([^&#]*)"));
    // eslint-disable-next-line no-restricted-globals
    var results = regex.exec(queryString);
    return results === null
        ? ''
        : decodeURIComponent(results[1].replace(/\+/g, ' '));
};
exports.getParameterByName = getParameterByName;
var handleLocalStorage = function (key, value) {
    try {
        localStorage.setItem(key, value);
    }
    catch (error) {
        console.error(error);
    }
};
exports.handleLocalStorage = handleLocalStorage;
