import { loadScript } from '@forbes/fbs-assets-loader';
export function initMoatYield() {
    try {
        loadScript('//z.moatads.com/forbesprebidheader122641196143/moatheader.js', function () {
            console.warn('MOAT script already loaded');
        });
    }
    catch (e) {
        console.error('Error loading MOAT script');
    }
}
export function targetYieldSlot(slot) {
    try {
        window['moatPrebidApi'].setMoatTargetingForSlot(slot);
    }
    catch (e) {
        console.error('Error targeting MOAT yield for slot', slot.getSlotElementId());
    }
}
//# sourceMappingURL=moat-bidding.js.map