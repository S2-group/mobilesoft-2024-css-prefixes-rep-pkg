"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.checkIsIOS = exports.checkIsTablet = exports.checkIsMobile = void 0;
/**
 * Compares browser user agent against known list of mobile devices
 * @param {String} userAgent Request UserAgent to compare
 * @returns {Boolean} Whether this user agent is considered mobile
 */
var checkIsMobile = function (userAgent) { return Boolean(userAgent && (userAgent.match(/iP(hone|od)/i)
    || userAgent.match(/BlackBerry/i)
    || (userAgent.match(/Android/i) && userAgent.match(/Mobile/i))
    || (userAgent.match(/Nokia/i) && userAgent.match(/Symbian|Windows Phone/i)))); };
exports.checkIsMobile = checkIsMobile;
/**
 * Compares browser user agent against known list of tablet devices
 * @param {String} userAgent Request UserAgent to compare
 * @returns {Boolean} Whether this user agent is considered a tablet device
 */
var checkIsTablet = function (userAgent) { return Boolean(userAgent && (userAgent.match(/Tablet|iPad/i))); };
exports.checkIsTablet = checkIsTablet;
/**
 * Compares browser user agent against known list of IOS devices.
 * @param {String} userAgent Request UserAgent to compare
 * @param {Window} window Browser window
 * @returns {Boolean} Whether this user agent is considered a tablet device
 */
var checkIsIOS = function (userAgent, window) {
    return Boolean(userAgent && window && (/iPad|iPhone|iPod/.test(userAgent) && !window.MSStream));
};
exports.checkIsIOS = checkIsIOS;
