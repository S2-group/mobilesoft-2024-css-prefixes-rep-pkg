'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

/**
 * Fires tracking function.
 *
 * @param trackingProps Tracking object that contains the tracking required properties.
 */
var handleEventTracking = function (trackingProps) {
    var track = trackingProps.track, category = trackingProps.category, action = trackingProps.action, label = trackingProps.label;
    track(category, action, label);
};
var trackingHandler = function (trackingProps, label) {
    if (trackingProps) {
        var trackingObject = {
            track: trackingProps.track,
            category: trackingProps.category,
            action: trackingProps.action,
            label: label,
        };
        handleEventTracking(trackingObject);
    }
};

exports.trackingHandler = trackingHandler;
//# sourceMappingURL=tracking.js.map
